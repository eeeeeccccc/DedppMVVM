<?xml version="1.0" encoding="utf-8"?><!--布局以layout作为根布局-->
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- data标签 -->
    <data>

        <variable
            name="presenter"
            type="com.dedpp.dedppmvvm.act.MainActivity.Presenter" />
    </data>

    <!--我们需要展示的布局-->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context="com.dedpp.dedppmvvm.act.MainActivity">

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{() -> presenter.baseDataBinding()}"
            android:text="@{`DataBinding基础使用` + presenter.message}"
            android:textAllCaps="false" />
    </LinearLayout>

</layout>